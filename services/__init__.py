"""Shared LedgerPulse libraries consumed by both the API and the worker.

Subpackages:
  - services.llm          LLM gateway, prompts, schemas, providers (mock/anthropic)
  - services.ocr          OCR provider interface + implementations
  - services.integrations Accounting/ERP adapter contract + mock adapter
"""
