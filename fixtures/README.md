# Fixtures

Synthetic, redacted demo data. **No real company or vendor data.**

- `invoices/` - the 8 demo invoices (INV-001..008) as PDFs/images (added in Phase 2+).
- `purchase_orders/`, `contracts/`, `work_orders/` - commercial context for matching (Phase 4).
- `expected/` - golden expected extraction/matching outputs the mock provider returns and the
  golden evals assert against.
