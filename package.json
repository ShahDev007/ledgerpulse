{
  "name": "ledgerpulse",
  "private": true,
  "version": "0.1.0",
  "description": "LedgerPulse — AI-native invoice intelligence platform (interview concept)",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:web": "pnpm --filter @ledgerpulse/web dev",
    "build:web": "pnpm --filter @ledgerpulse/web build",
    "lint": "pnpm --filter @ledgerpulse/web lint"
  }
}
